# 公開手順（Render でのデプロイ）

このサイトは動的アプリ（Node.js）なので、**GitHub Pages では公開できません**。
ここでは初心者向けに、無料で使える **Render** での公開手順を説明します。

---

## まず知っておくこと（無料枠の制限）

| | 無料プラン (free) | 有料プラン (starter〜) |
|---|---|---|
| 費用 | 0円 | 月 $7〜 |
| アップロード画像・お問い合わせ | **再デプロイ/再起動で消える** | 永続ディスクで残せる |
| しばらくアクセスが無いと | スリープ（次回表示が少し遅い）| 起動したまま |

> まず**無料でお試し → 本格運用したくなったら有料に切り替えてデータを残す**、という流れがおすすめです。

---

## 手順（無料で公開）

### 1. リポジトリを Render に接続
1. [https://render.com](https://render.com) にアクセスし、GitHub アカウントでサインアップ／ログイン
2. ダッシュボードで **New → Blueprint** を選択
3. このリポジトリ（`kay-art-site-pj`）を選ぶ
4. リポジトリ内の `render.yaml` が自動で読み込まれます → **Apply**

### 2. パスワードを設定
- 途中で **`ADMIN_PASSWORD`** の入力を求められます（管理画面のログインパスワード）。
  好きなパスワードを入力してください。※この値は公開されません。
- `SESSION_SECRET` は自動生成されるので入力不要です。

### 3. デプロイ完了 → URLを開く
- 数分でビルドが終わり、`https://aquarelle-portfolio.onrender.com` のようなURLが発行されます。
- 公開サイト：そのURL
- 管理画面：そのURL + `/katoasao/`（例：`https://....onrender.com/katoasao/`）

### 4. 作品を登録してみる
1. 管理画面を開き、設定したパスワードでログイン
2. 「作品の管理」タブから写真をアップロード
3. トップページに作品が表示されればOK

---

## データの永続化について（現在の設定）

`render.yaml` は現在 **Starterプラン + 永続ディスク（/var/data）** の構成になっています。
アップロード画像・お問い合わせ・編集した文章は永続ディスクに保存され、**再デプロイ・再起動しても消えません**。

- Blueprintを **Apply**（または再同期）すると、Renderがプラン変更の承認（支払い）を求めます。承認すると有料プランで起動します。
- 料金の目安：Starter 約 $7/月 ＋ ディスク1GB 約 $0.25/月（最新の金額は Render の Pricing を確認）。

### 無料(free)に戻したい場合
`render.yaml` で `plan: free` に変更し、`disk` と `DATA_DIR` / `UPLOAD_DIR` の3項目を削除してください（※無料に戻すとデータは再び消える仕様になります）。

---

## 動作確認（GA4 のリアルタイム）

公開できたら、Google Analytics の **「リアルタイム」レポート**で、自分のアクセスがカウントされるか確認してください（数十秒で反映されます）。

---

## 管理画面「アクセス解析」タブの設定（GA4 Data API）

管理画面の「アクセス解析」タブは、Google Analytics 4 の **Data API** から実際の数値を取得して表示します。
設定しなくてもサイトは問題なく動きますが、タブには「未設定」の案内が表示されます。

### 1. GA4 プロパティID を確認する
GA4（`public/js/analytics.js` の測定ID `G-XXXXXXX` を設定した同じプロパティ）の管理画面で、
「管理 → プロパティ設定」を開くと **プロパティID**（数字のみ、例：`123456789`）が表示されます。
これを環境変数 `GA4_PROPERTY_ID` に設定します。

### 2. サービスアカウントを作成し、GA4 に「閲覧者」権限で追加する
1. [Google Cloud Console](https://console.cloud.google.com/) で適当なプロジェクトを作成（または既存のものを使用）
2. 「APIとサービス → ライブラリ」で **Google Analytics Data API** を有効化する
3. 「IAMと管理 → サービスアカウント」で新規サービスアカウントを作成する（役割の付与は不要）
4. 作成したサービスアカウントの「鍵を追加 → 新しい鍵を作成 → JSON」でキーファイルをダウンロードする
   （メールアドレスが `xxxx@xxxx.iam.gserviceaccount.com` の形式のものです）
5. GA4 管理画面の「管理 → プロパティのアクセス管理」で、上記のサービスアカウントのメールアドレスを
   **「閲覧者」(Viewer)** 権限で追加する
6. ダウンロードしたJSONファイルの中身をまるごと、環境変数 `GA4_SERVICE_ACCOUNT_JSON` に設定する
   （改行を含む値を1行でしか入力できない環境では、ファイルの中身を base64 エンコードした文字列でも構いません）

Renderの場合は、ダッシュボードの Environment タブでこの2つの環境変数を追加してください
（`render.yaml` には `sync: false` で項目だけ用意してあります）。

### 3.「作品別 閲覧数」を有効にするための追加設定（任意）
作品ごとの閲覧数ランキングは、作品を開いたときに送信されるカスタムイベント `view_work`
（パラメータ `work_id`）を集計しています。GA4側でこのパラメータをレポートに使えるようにするには、
**1回だけ**手動設定が必要です。

1. GA4 管理画面の「管理 → カスタム定義 → カスタムディメンションを作成」を開く
2. 以下の内容で登録する：
   - ディメンション名：`work_id`（任意の名前でも可）
   - 範囲：**イベント**
   - イベントパラメータ：`work_id`
3. 登録後、実際にサイトで作品を開く（閲覧イベントが発生する）と、半日〜1日ほどでランキングに反映され始めます。

この設定をしていない間は、「作品別 閲覧数」の枠だけ空欄（案内文）になりますが、他のパネル
（総訪問者数・流入元・リアルタイム・国別・新規/リピーターなど）は通常どおり表示されます。

---

## 管理画面「更新履歴」タブでGitHubの履歴を表示する（GITHUB_TOKEN）

更新履歴タブは、GitHubのコミット一覧APIからサイトの改修・修正の履歴を取得して表示します。
**このトークンを設定しないと、GitHubの履歴が表示されず「サイトの情報更新」だけになります。**

理由：GitHub APIは未認証だと **1時間あたり60回まで** に制限され、しかもこの上限は
**IPアドレス単位**です。Render等の共有サーバーでは、同じIPを使う他のサービスと合算で
すぐ上限に達し、`403（API制限）`が返ってGitHubの履歴が取得できなくなります。
トークンを設定すると認証済み扱いになり、上限が **1時間あたり5000回** に緩和されて安定します。

### 設定手順
1. GitHubにログインし、[Settings → Developer settings → Personal access tokens → Tokens (classic)](https://github.com/settings/tokens) を開く
2. 「Generate new token (classic)」で新規作成する
   - 対象リポジトリが**公開**なら、**スコープ（権限）は何もチェックしない**でOK（公開情報の読み取りだけで十分）
   - 対象リポジトリが**非公開**の場合のみ、`repo` にチェックを入れる
3. 生成された文字列（`ghp_...`）をコピーし、Renderのダッシュボードの Environment タブで
   環境変数 `GITHUB_TOKEN` に貼り付けて保存する（`render.yaml` に `sync: false` の項目を用意済み）
4. 保存すると再デプロイされ、更新履歴タブにGitHubの履歴が表示されるようになります

※ 対象リポジトリを変えた場合は、環境変数 `GITHUB_REPO`（例：`owner/repo`）も合わせて設定してください。

---

## ローカルでの確認方法（公開前のテスト）

```bash
npm install
ADMIN_PASSWORD=好きなパスワード npm start
# → http://localhost:3000 と http://localhost:3000/katoasao/
```
